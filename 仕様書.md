# 駐車場自動配置ツール 仕様書

本書は、ブラウザだけで動作する「駐車場自動配置スタディツール」を、コードを参照せずにゼロから再構築するための要件・仕様書である。LLM(Claude Opus / Gemini 等)への実装指示にそのまま使えるよう、座標系・数式・処理順序・受入条件を明記する。

---

## 1. 目的と全体像

- 計画初期の**概算検討用ツール**。敷地ポリゴンに対して平面の乗用車駐車場レイアウト(車室・車路・緑地)を自動生成し、駐車台数と面積内訳を算出する。
- 敷地はマウス作図、または**DXF下図をなぞって**入力する。
- 生成結果は画面表示のほか、**DXF / PNG / JSON** で書き出せる。

### 技術制約(必須)

| 項目 | 制約 |
|---|---|
| 形態 | **単一HTMLファイル**(CSS/JSすべてインライン) |
| 依存 | 外部ライブラリ・CDN・フォント読込一切なし。バニラJS + Canvas 2D |
| 動作 | サーバー不要。`file://` でダブルクリック起動して全機能が動くこと |
| データ | すべてブラウザ内で処理(ファイルは FileReader で読む。外部送信なし) |
| 起動時 | **何も入力されていない空の状態**から始まる(プリセット・サンプルなし) |

### 単位・座標系

- 内部モデルは**メートル単位・Y軸上向き**の世界座標。画面表示はビュー変換(`screen.x = ox + wx·s`, `screen.y = oy − wy·s`)で行う。
- 敷地等の図形確定後に**ビューを勝手に動かさない**(自動フィット禁止)。フィットは「全体」ボタンでのみ実行。
- Canvas は devicePixelRatio 対応(内部解像度 = CSS px × dpr)。

---

## 2. 画面構成

```
┌──────────────────────────────────────────────┐
│ ヘッダー: タイトル ｜ 台数・原単位のインライン表示           │
├──────────┬──────────────────────┬─────────────┤
│ 左パネル      │ キャンバス                  │ 右パネル        │
│ (300px)     │  左上にツールバー            │ (238px)       │
│  設定・操作    │  左下にメッセージ行           │  結果・比較案    │
└──────────┴──────────────────────┴─────────────┘
```

ダークテーマ。参考配色: 背景 `#14181d`、パネル `#1c2127`、罫線 `#313a44`、文字 `#d7dee5`、アクセント(琥珀) `#ffc857`、シアン `#7fd1e8`、赤 `#e07a6b`。

### 2.1 左パネル(上から順)

1. **下図(DXF)**
   - ボタン: `DXF読込` / `2点校正`(読込前は無効) / `クリア`(読込前は無効)
   - `下図を表示` チェック、`濃度` スライダー(10–100、初期55)、`図面単位` セレクト(mm/cm/m/inch)
   - レイヤー一覧(チェックで表示切替)
   - ヒント文: ASCII DXFのみ対応、DWG・バイナリDXF非対応
2. **全消去ボタン**(赤系・大型・独立枠)。敷地・障害物・手動車路・下図DXFをすべて消去
   - ※プリセット(矩形・L字・座標入力)は**設けない**
   - ※入力状況の件数表示(`障害物: n件` 等)とカテゴリ別クリアは**設けない**。
     図形の削除は選択モードで対象を選び Delete、取り消しは Undo(§3.6)で行う
3. **車室**: 車室幅(初期 **2.5m**)、車室奥行(初期 **5.0m**)、駐車角度セレクト(**90°/60°/45°**、初期90°)
4. **車路(自動帯)**: 車路幅(初期 **6.0m**)。※通行方式の選択は**設けない**
   - **外周部の扱い** セレクト: `外周車路` / `外周車室`(**初期値**) / `なし`
5. **手動車路**: 幅員(初期 **6.0m**)。未選択時は新規作図の既定値。
   選択モードで手動車路を選ぶとその車路の幅員が表示され、値を変更すると図に反映される
6. **外周緑化帯**: 緑化幅(初期 **1.0m**、0で無効)
7. **配置方向**: `自動最適化` チェック(初期ON)。OFF時のみ角度スライダー(0–179°)+数値入力が有効になり、**リアルタイム**に再計算・再描画
8. **レイアウト生成** ボタン(強調表示)
9. **書き出し**: DXF単位セレクト(mm[初期]/m)、`DXF` `PNG` `保存` `読込` ボタン

各数値入力の有効範囲と検証は §2.4。

パラメータ変更(車室幅・奥行・角度・車路幅・外周部・緑化幅)は、敷地入力済みなら**デバウンス(約180ms)付きで自動再生成**する。

### 2.4 入力検証(必須)

数値入力は空欄・非数値・範囲外になり得る。**特に車室幅が0になると `pitch = W/sinα = 0` となり、配置ループ
`for (x=…; x<=…; x+=pitch)` が進まず無限ループでブラウザが固まる。** 次の2段構えで防ぐ。

| 入力 | 下限 | 上限 | 既定 |
|---|---|---|---|
| 車室幅 | 1.5 | 5 | 2.5 |
| 車室奥行 | 3 | 15 | 5 |
| 車路幅 | 2.5 | 30 | 6 |
| 手動車路の幅員 | 2.5 | 30 | 6 |
| 緑化幅 | 0 | 30 | 1 |
| 配置方向 | 0 | 179 | 0 |

1. **計算用の値は常にクランプする**(`readParams()` 等が使う取得関数側で行い、**DOMは書き換えない**)。
   これにより入力途中の値でもエンジンへ0やNaNが渡らない
2. **入力確定時(`change`)にDOMを是正する** — 空欄・非数値は既定値、範囲外はクランプし、
   `setMsg()` で「○○は a〜b の範囲で入力してください（c に修正しました）」と通知する
   - **`input` イベントでは是正しないこと。** 入力途中に書き換えると「15」を打つために「1」を経由できなくなる
3. セレクト(駐車角度・外周部の扱い・DXF単位)は、選択肢に無い値なら既定値へ戻す
4. HTMLの `min`/`max` 属性も設定する(スピナー操作の抑止。ただしそれだけでは不十分)

**JSON読込時の検証** — 外部ファイルは信用しない。

- 座標が数値でない／点数が下限(ポリゴン3点・ポリライン2点)未満の図形は**採用せず除外**する
- 配列でない要素・オブジェクトでない要素も除外する
- パラメータは上表で是正し、選択肢外の値は既定値へ戻す
- 除外・修正した件数をメッセージに併記する(`／⚠ 不正な図形5件を除外・範囲外のパラメータ8件を修正`)
- 手動車路の幅員が0や非数値なら、左パネルの既定値を使う

### 2.2 ツールバー(キャンバス左上)

`敷地` / `障害物` / `手動車路` / `編集` / **`↶`(元に戻す) / `↷`(やり直す)** / `全体`。**起動時は「敷地」が選択状態**。
`↶` `↷` は区切り線を挟んで `全体` の左に置き、**対応するスタックが空のときは `disabled`** にする(§3.6)。

### 2.3 右パネル

- **駐車台数**(大きく表示)
- 統計テーブル: 敷地面積 / 原単位(㎡/台) / 車室面積計 / 車路面積(参考) / 障害物面積 / 緑地面積・緑地率 / 配置方向 / 探索案数・時間
- **方向別 上位案**(上位5件)。クリックでその案に表示を切替

**数値表示のガード(必須)**: 統計テーブルの全項目は、値が有限でない場合(0除算・`NaN`)は必ず `–` を表示する。`NaN` `Infinity` `undefined` を画面に出してはならない。

- 原単位 = 敷地面積 ÷ 台数 … **台数0のとき** `–`
- 緑地率 = 緑地面積 ÷ 敷地面積 … **敷地面積0のとき**(自己交差敷地・直線状の退化敷地)は面積のみ表示し、率は `–`
- 共通の整形関数を1つ設け(`isFinite` でないとき `–` を返す)、全項目をそれ経由で表示する

---

## 3. 入力操作仕様

### 3.1 パン・ズーム(全ツール共通)

- **マウスホイール**: カーソル位置を中心に拡大縮小(1ノッチ ×1.12 / ÷1.12)
- **中ボタンドラッグ**: パン
- 編集ツール中は「何もない場所の左ドラッグ」でもパン

### 3.2 作図(敷地・障害物・手動車路)

- クリックで頂点追加。作図中は確定済み線に加えて**最終点→カーソルのラバーバンド**(破線)を表示
- **寸法表示**: 確定済み各セグメントおよびラバーバンドの**中央付近に長さラベル**(`12.34 m`、等幅フォント、暗背景の小箱)を必ず表示
- 閉合/確定: 敷地・障害物(閉ポリゴン、3点以上)は「始点クリック(10px以内)/ダブルクリック/Enter」。手動車路(ポリライン、2点以上)は「ダブルクリック/Enter」
- **自己交差の拒否**: 敷地・障害物の閉合時に自己交差を判定し、交差していれば**閉合しない**。メッセージ行に「辺が交差しています。頂点を修正してください」と表示し、**作図中の点列は保持**してユーザーが修正できるようにする。手動車路(中心線)は自己交差してよいため判定しない(詳細は §3.5)
- Esc で作図中止(点列破棄)
- **スナップ**: 下図表示中は下図の頂点に8px以内で吸着。それ以外は0.1m丸め
- 確定してもビュー(位置・縮尺)は変えない

### 3.3 編集ツール

- 頂点ドラッグで移動(スナップ有効)。ドロップ時に自動再生成
- 辺の中点ハンドルをドラッグ、または辺上ダブルクリックで頂点追加
- 頂点を選択して Delete/Backspace、または頂点を右クリックで削除(下限: ポリゴン3点・ポリライン2点)
- 手動車路の中心線クリック(または車路帯の内部)で選択 → **左パネルの「手動車路」欄**に幅員が表示され、変更すると図に反映。削除は Delete
- 図形クリックで選択・ドラッグで移動。Shift+クリックで同種図形を複数選択(複数選択中は移動のみ)
- 頂点移動・頂点削除・図形移動の**結果**が自己交差になった場合は警告のみ表示し、**操作は取り消さない**(続けて修正できるようにする。詳細は §3.5)

### 3.4 手動車路のデータ

`{ pts: [{x,y}...], width: 作図時の車路幅(m) }`。中心線+幅から**車路帯ポリゴン**を生成する(マイター接合、鋭角はベベル、端部フラット)。

### 3.5 自己交差の検出

8の字型など自己交差するポリゴンは、符号付き面積が打ち消し合って**面積0㎡・台数0台**という無意味な結果になる。原因がユーザーに伝わるよう、次のとおり検出して知らせる。

- **判定**(UI層。配置エンジンからは独立):
  閉ポリゴンの**非隣接**辺ペアすべてについて線分交差を判定する。端点を共有する辺(隣接辺、および「最初の辺×最後の辺」)は判定から除外する。頂点数は実用上100未満のため O(n²) で十分。
- **対象**: 敷地・障害物のみ。**手動車路は中心線のため判定しない**
- **タイミングと挙動**:

  | 契機 | 挙動 |
  |---|---|
  | 作図の閉合(敷地・障害物) | **閉合を拒否**。点列は保持し、メッセージで修正を促す |
  | 頂点移動の確定 / 頂点削除 / 図形移動の確定 | 警告のみ。操作は取り消さず再配置も行う |
  | レイアウト生成 | 生成完了メッセージに `／⚠ …の辺が交差しています` を併記する |

### 3.6 Undo / Redo

**スナップショット方式**(差分ではなく全体コピー)で実装する。

- **対象**: `site` / `obstacles` / `aisles` の**図形3種のみ**。ビュー(倍率・位置)・下図DXF・選択状態・パラメータ入力欄は**対象外**
- **コピー**: `JSON.parse(JSON.stringify(...))` 相当の**ディープコピー**。積むときも復元するときもコピーし、**頂点オブジェクトをスタックと共有しない**
- **スタック**: `undoStack` / `redoStack` の2本。上限**各50件**(超えたら古いものから捨てる)。新規操作を積むとき `redoStack` はクリアする
- **積むタイミング(操作の直前。1操作＝1スナップショット)**:
  作図の閉合・確定 / 頂点の追加(辺ダブルクリック) / 頂点の移動確定 / 頂点の削除 / 図形の移動確定 / 図形の削除 / 手動車路の幅員変更 / 「全消去」 / JSON読込・自動保存の復元
  - **ドラッグ中の各フレームでは積まない**。ドラッグ開始時(`mousedown`)にスナップショットを採って保持し、`mouseup` で**実際に移動していたときだけ**積む
  - 閉合が拒否された場合(自己交差・点数不足)は積まない
- **実行時の処理**: 状態を復元 → `sel=null` → 再配置(デバウンス) → 再描画。敷地が無い状態へ戻った場合は台数・比較案の表示もクリアする
- **キーボード**: `Ctrl+Z`=Undo、`Ctrl+Y` および `Ctrl+Shift+Z`=Redo。**入力欄(INPUT/SELECT/TEXTAREA)にフォーカスがあるときは横取りせず**ブラウザ標準動作を優先する(既存のDeleteキーと同じガード)
- スタックが空のときは「これ以上戻せません」/「これ以上やり直せません」と表示する

---

## 4. 配置エンジン仕様

### 4.1 パラメータ

| 記号 | 意味 | UI初期値 |
|---|---|---|
| W | 車室幅 | 2.5 m |
| D | 車室奥行 | 5.0 m |
| α | 駐車角度 | 90° |
| A | 車路幅 | 6.0 m |
| g | 緑化幅 | 1.0 m |
| perimeter | 外周部の扱い | `stall`(外周車室) |

派生値: `perimeterDepth = (perimeter=stall) ? D + A : (perimeter=ring) ? A : 0`

### 4.2 方向探索(自動最適化)

1. 候補方向 θ = 敷地各辺の方位角(mod 180°) ∪ {0,15,30,…,165}(0.5°丸めで重複除去)
2. 各 θ について、帯周期 `P = 2·Deff + A`(`Deff = D·sinα + W·cosα`)の範囲でオフセットを 0.5m 刻みに全探索
   - **この全探索は簡略化してはならない**。台数はオフセットに対して連続的に変化せず、「車室列がもう1列収まるか否か」で**段差状・スパイク状**に変化するため、粗探索で当たりを付けてから細探索する方式では最良案を取り逃す(実測結果は §11.1)
3. 台数最大の案を採用。θ別ベストの上位5案を比較用に保持
4. 自動最適化OFF時は指定 θ のみ探索(オフセットは全探索)

### 4.3 1案の評価(回転フレーム)

敷地重心 c 周りに **−θ 回転**した座標系で計算し、結果を +θ で世界座標へ戻す。

- 車路帯: 水平帯 `y∈[yLo, yHi]`, `yLo = offset + k·P + Deff`, `yHi = yLo + A`(k は bbox を覆う整数範囲)
- 各帯の上下に車室列。x を `pitch = W / sinα` 刻みに走査
- **車室ポリゴン**(前面2点が車路側)。90°時は幅W×奥行Dの矩形が帯に接する。斜め(α<90°)時の頂点は次式(up=帯が上にある列):
  ```
  up:  p1=(x, yLo−W·cosα), p2=p1+(W·sinα, W·cosα),
       p3=p2+(D·cosα, −D·sinα), p4=p1+(D·cosα, −D·sinα)
  down: y符号を反転(帯 yHi の上側に展開)
  ```
- **車室の採用条件**(すべて必須):
  1. サンプル点(4頂点+各辺中点+重心)がすべて敷地内
  2. 敷地境界からの距離 ≥ `g + perimeterDepth`(=外周帯と緑化帯を空ける)
  3. 障害物・手動車路帯に掛からない(相互に頂点包含もチェック)
  4. **前面アクセス帯**: 車室前面から車路方向に幅pitch×奥行Aの矩形が、敷地内(縁からg以上)かつ障害物に掛からない(手動車路との重なりは許容)
- **帯の実長**: 採用車室のx範囲(min/max)を帯の `x0/x1` とする(=**帯は最端車室で止める**。緑地を無駄に分断しない)

### 4.4 外周車室モード(`perimeter = stall`)

敷地境界沿いに**外向き駐車の車室**を一周配置し、その内側に周回車路(幅A)を確保する。**方向探索とは独立**に一度だけ計算し、全方向案に合算する。

1. 基準線 = 敷地を緑化幅 g だけ内側オフセットしたポリゴン(CCWに正規化)
2. **凸角の隅後退**: 内角 θint の凸頂点では、辺の両端をそれぞれ `D / tan(θint/2)` 後退させる(隣接辺の車室・出庫動線との干渉防止)。凹角・直線は後退0
3. 各辺: 有効長 `avail = 辺長 − 両端後退`。`n = floor(avail / W)` 台を**中央寄せ**で配置(余りは両端の緑地)
4. 車室 = 基準線上の背面2点 + 内側法線方向にD進めた前面2点(前面が車路側)
5. **出庫保証**(車を出せない車室を作らない):
   - 前面に奥行A(車路幅)のアクセス帯を設け、敷地・障害物と干渉しないこと
   - 車室が既存の車室・**他車室のアクセス帯**と交差しないこと(凸多角形SAT、接触は許容)
   - 自身のアクセス帯が既存車室と交差しないこと
6. 内部配置(4.3)の車室境界マージンは `g + D + A` になるため、外周車室帯と内部配置は干渉しない

### 4.5 後処理: 車路帯の接続と隅切り(finishA)

生成結果(表示する案)に対して一度だけ実行する。

**帯端の延長(緑地の切断)** — 各帯の左右端について:
- 端から外側へ 0.25m 刻み(最大40m)で走査。断面サンプル(y0+0.15 / 中央 / y1−0.15 の3点)を判定
- 敷地外 or 障害物に当たったら**中止**(延長しない)
- 全サンプルが「外周帯ゾーン(境界からの距離 < g+perimeterDepth、perimeter≠なし の場合)」または「手動車路帯内」になったら**接続成功**とし、帯端をそこまで延長する

**行き止まりの検出** — 接続に失敗した帯端(`connected` が決まらなかった端)の数を数え、結果オブジェクトに `deadEnds` として持たせる。
エンジン側は**数えるだけ**にとどめ、警告表示はUI層で行う(§5.1)。1つの帯は左右2端を持つため最大 `帯数×2`。

**隅切り** — 外周帯に接続した端について、帯側縁(y=y0, y=y1 の線)と「外周帯の内側ライン(境界から g+perimeterDepth 内側)」の交点 P を求め:
- 2本のレイ: r1 = 帯側縁に沿って帯内部へ戻る向き、w = 内側ラインに沿って緑地側(帯から離れる側)
- 交角 deg = ∠(r1, w) が **15° < deg ≤ 120°** のとき、`d = 1 / sin(deg/2)` として三角形 `[P, P+r1·d, P+w·d]` を隅切りとする(**底辺=斜辺の弦長がちょうど2m**になる)
- 車室と交差する隅切りは棄却
- 隅切り三角形は車路として描画・集計する

---

## 5. 描画仕様(レイヤー順)

1. 1mグリッド `#191f26` / 5mグリッド `#222a33`(1目盛7px未満なら省略)
2. DXF下図(グレー `#6a7d8d`、濃度スライダー値の透明度)
3. 敷地内部(クリップして描画):
   - 下地 `#20272f` → **敷地内全面を緑地色 `#435F52`(不透明)で塗る**
   - perimeter≠なし: 境界に沿って幅 `2·(g+perimeterDepth)` のストローク `#2c3742`(=外周帯を暗色に)
   - その上に幅 `2·(g + (stallモード ? D : 0))` のストローク `#435F52`(=緑化帯を復元。外周車室モードでは車室帯の余白も緑)
   - 自動車路帯(回転を戻した矩形)と隅切り三角形を `#2c3742` で塗る
   - 手動車路帯を `#2c3742` で塗る(選択中は縁 `#ffc857`)
4. 敷地輪郭 `#9fb2c2` 1.6px
5. **車室**: 不透明下地 `#20272f` → 半透明シアン塗り `rgba(127,209,232,.10)` + 縁 `rgba(127,209,232,.85)`。**前面(車路側)の辺のみ太さ2pxの琥珀 `rgba(255,200,87,.9)` で強調**(=車の向きが分かる)
6. 障害物: 不透明下地 + 赤系半透明塗り、赤破線縁
7. 手動車路の中心線(破線)、作図中図形+寸法ラベル、編集ハンドル

色の意図: **緑地=#435F52 一色(不透明)**、**車路=#2c3742 一色**。「車路と車室の間の余った部分」は上塗りされずに自動的に緑地色で残る、という減算的な描画構造にすること。

### 5.1 警告表示(キャンバス右上)

計画上の問題を1つの警告領域にまとめて表示する。**複数の警告は同時に表示できること**(上書きしない)。

| 条件 | 文言 |
|---|---|
| 障害物と手動車路が干渉 | `⚠ 障害物と手動車路が干渉しています（形状を確認してください）` |
| `deadEnds > 0`(§4.5) | `⚠ 行き止まりの車路が○箇所あります（動線を確認してください）` |

- 判定・表示は**レイアウト生成後(`finishA` の後)**に行う。`deadEnds` は `finishA` で確定するため、生成前に評価してはならない
- 比較案(方向別 上位案)を切り替えたときも、その案について再評価する
- 敷地の削除・全消去・Undoで結果が消えたときは警告も消す
- **外周部の扱いが「なし」のときは接続先の車路が存在しないため、全帯端が行き止まりとして報告される**(仕様どおり。周回車路が無い計画である旨の注意喚起として機能する)

---

## 6. 面積集計仕様

0.5mグリッドのサンプリングで各セルを**排他的に1分類**する。判定順序が仕様(上が優先):

1. 敷地外 → 対象外
2. 障害物内 → 障害物面積
3. 境界距離 < g → 手動車路帯内なら車路、そうでなければ**緑地**(緑化帯)
4. 手動車路帯内 → 車路
5. **車室内 → 車室**(※外周帯ゾーンより先に判定。外周車室を車路にしないため)
6. stallモード かつ 境界距離 < g+D → **緑地**(外周車室帯の余白)
7. perimeter≠なし かつ 境界距離 < g+perimeterDepth → 車路(外周車路)
8. 自動車路帯(回転フレームで x0≤x≤x1, y0≤y≤y1) → 車路
9. 隅切り三角形内 → 車路
10. 残り → **緑地**(車路と車室の間の余剰はすべて緑地)

表示: 車室面積計は解析値 `台数×W×D`。緑地は面積と敷地面積比(%)を併記。
**検算条件: 緑地+車路+車室+障害物 ≒ 敷地面積(グリッド近似で誤差±1%以内)**

**実装**: 「全セル × 全ポリゴン」で内外判定すると車室数×セル数のオーダーになる(実測992万回)。
障害物・手動車路・車室・隅切りは**ポリゴンごとに外接矩形内のセルだけを走査してフラグ配列へ展開**し、
セル走査ではフラグを参照する(§11.2(d))。**上記の優先順位は変えないこと。**

---

## 7. DXF入出力仕様

### 7.1 読込(下図)

- ASCII DXF をグループコード2行ペアで自前パース。対応: `LINE` / `LWPOLYLINE`(bulge円弧対応: θ=4·atan(bulge)で分割) / `POLYLINE`+`VERTEX`+`SEQEND` / `ARC` / `CIRCLE`(円弧は10°程度で折線化)
- **セクション追跡(必須)**: `0/SECTION` の直後の `2/<名前>` でセクション名を保持し、`0/ENDSEC` でクリアする。**エンティティの解析・読み飛ばしの計上は、セクション名が `ENTITIES` のときのみ**行う。`HEADER` / `TABLES` / `BLOCKS` / `OBJECTS` の内部データは解析も計上もしない
  - 例外: `$INSUNITS` の取得は、セクション追跡に巻き込まれないようファイル全体に対する事前スキャンで行う(HEADERセクション内のため問題ない)
- `$INSUNITS` を読んで単位初期値(1=inch, 2=ft, 4=mm, 5=cm, 6=m。不明時mm)。単位はUIで変更可能
- 読込時に bbox 最小点を原点へ平行移動。レイヤー別表示切替、濃度変更、**2点校正**(2点クリック→実距離入力→p1を不動点にスケーリング)
- 下図の全頂点をスナップ点として登録
- ファイル選択とキャンバスへの**ドラッグ&ドロップ**の両対応
- 未対応エンティティは種類と件数をメッセージ表示(`下図読込: ○○.dxf（294要素／読み飛ばし: TEXT×1）`)。
  ただし**報告するのは作図系エンティティのみ**とし、以下のリストに限る(これ以外は無視する):
  `INSERT, TEXT, MTEXT, ATTDEF, SPLINE, ELLIPSE, HATCH, DIMENSION, LEADER, MLEADER, MLINE, SOLID, 3DFACE, POINT, REGION, BODY, 3DSOLID, IMAGE, WIPEOUT, XLINE, RAY`
  - 理由: 除外リスト方式(既知の非図形タイプを列挙して残りを報告する)にすると、`DICTIONARY` `SCALE` `VISUALSTYLE` `CELLSTYLEMAP` などCADが書き出す内部データが大量に並び、**本当に読み込めなかった実図形がノイズに埋もれる**。報告対象を列挙する方式にすること

### 7.2 書き出し

R12互換 ASCII(ENTITIESセクションのみ)。単位係数(mm=1000/m=1)を全座標に乗算。**画面表示と一致した線のみ**出力すること(以下が仕様の核心):

| レイヤー | 内容 |
|---|---|
| SITE | 敷地境界(閉) |
| GREEN | **緑地の外形線**(下記の切り欠き処理済みポリライン群) |
| OBSTACLE | 障害物(閉) |
| STALL | 車室1台ごと(閉) |
| AISLE_MANUAL | 手動車路帯の輪郭(敷地内にクリップ、車路同士の重なり部は相互に消去) |
| AISLE | 自動車路帯の側縁・端部線、隅切りの弦(LINE) |
| ANNOTATION | `PARKING STALLS: n (xx.x m2/stall)` のTEXT(高さ1m、敷地左上に配置) |

**GREEN(緑地外形)の生成規則:**
- 緑化帯内側ライン = 敷地の g 内側オフセット。ただし stallモードでは g+D 内側(=外周車室帯の内側・周回車路の外縁)とし、**車室と重なる区間は切り欠く**(車室ポリゴンを重心基準で2%拡大したものを穴として使うと縁の共線に頑健)
- 外周帯の内側ライン(g+perimeterDepth 内側オフセット)も出力し、**自動車路帯の取り付き口・手動車路の横断部・障害物部分を開口**する
- 実装: 「閉ループの各辺 → keepポリゴン内にクリップ → 穴ポリゴン群内部を除去 → 端点共有(許容差1e-4)で線分をポリラインに連結」という汎用処理を作る

**AISLE(自動車路線)の規則(突き抜け防止):**
- 帯の側縁(y0/y1)・端部線(x0/x1)を世界座標に戻し、**外周帯の内側ラインでクリップ**(外周車路に入り込む線を出さない)し、さらに手動車路帯・障害物の内部区間を除去
- 外周に接続した端部線は自動的に消える(クリップ範囲外のため)

### 7.3 PNG / JSON

- **PNG**: 画面のズーム・スクロール位置に関わらず、**図形全体(敷地・障害物・手動車路・車室・隅切り・下図)が収まる範囲**を出力する。画面キャンバスをそのまま `toDataURL` してはならない(ズーム中は図面が切れる)
  - オフスクリーンの `canvas` に描画してから出力する。既存の描画関数を再利用し、`state.view` と描画先を一時的に差し替える方式でよい。**`try/finally` で必ず元に戻す**こと(出力後に画面の表示倍率・スクロール位置が変化しないこと)
  - 出力サイズ: 図形の外接矩形の縦横比に合わせ、**長辺1200論理px**。これを**固定倍率2倍**で描画する(`devicePixelRatio` とは独立)。図形が無い場合は画面キャンバスと同サイズ
  - 余白: 外接矩形の周囲に **min(幅,高さ)の5%(最低20px相当)**
  - 画面は背景が透過だが、PNGは背景色 `#14181d` を塗る。配色・スタイルは画面表示と同一(ダークテーマ)
  - 下絵(DXFアンダーレイ)は現在の表示設定(表示ON/OFF・濃度・レイヤ)に従う。スナップマーカーは出力しない
  - レイアウト未生成(敷地のみ)・図形なしでも例外を出さずに出力できること
- 保存JSON: `{version, params{stallW,stallD,angle,aisle,aisleM,perimeter,green,autoDir,dir,expUnit}, site, obstacles, aisles}`。下図DXFは含めない(読込時にメッセージで案内)。読込で全パラメータ・図形を復元し再生成
- JSONの**構築と復元は関数として独立させる**(自動保存・自動保存の復元で共用するため)

### 7.4 自動保存とページ離脱の警告

タブを閉じる/リロードで作業内容が失われるのを防ぐ。

**自動保存**
- 保存先: `localStorage`、キーは文字列定数 `parking-tool-autosave`
- 保存内容: **§7.3の保存JSONと同一構造**に、保存時刻 `savedAt`(ISO文字列)を追加したもの。**下図DXFは含めない**
- タイミング: 図形またはパラメータが変化したとき。連続変更をまとめるため**2秒のデバウンス**をかける
- 敷地が未入力かつ障害物・車路も空のときは**保存しない**(空データで上書きしないこと)
- `localStorage` が使えない環境(プライベートモード等)で例外を投げないよう **`try/catch` で保護**する

**復元**
- ページ読み込み時に自動保存データがあれば、**自動では復元せず**、メッセージ領域に
  「前回の作業(○月○日 ○○:○○)を復元しますか？」と**復元ボタン**を表示する
- ユーザーが押したときのみ復元する(§7.3の復元処理を再利用)。復元は Undo で取り消せる
- メッセージ領域は `pointer-events:none` のため、**復元ボタンには `pointer-events:auto` を指定**すること(押せなくなる)
- 復元しないまま作図を始めた場合、自動保存が上書きされる(それでよい)。その時点で復元の案内は消す

**離脱警告**
- **最後の「保存」ボタン押下以降に図形が変化している**状態で離脱しようとしたとき、`beforeunload` で確認ダイアログを出す
- 図形が空のときは警告しない。「保存」ボタンでJSON保存した直後、およびJSONファイルを読み込んだ直後は警告しない
- パラメータのみの変更は自動保存の対象だが、離脱警告の対象**外**とする

---

## 8. 幾何ユーティリティ(自前実装)

外部ライブラリ禁止のため以下を実装する:

- `ptInPoly`: レイキャスト法
- `distPtSeg` / `distToPolyEdge`: 点と線分・ポリゴン縁の距離
- `polyArea`(符号付き・シューレース) / `centroid` / `bbox` / 点回転
- `convexIntersect`: SATによる凸多角形交差(eps>0で「接触は非交差」)
- `insetPolygon`: マイター内側オフセット(CCW正規化、マイター長>8dでベベル、面積が増える/消滅する場合はnull)
- `corridorFromPolyline`: 中心線+幅→帯ポリゴン(マイター、鋭角ベベル、端部フラット)
- `clipSegToPoly` / `segMinusPolys`: 線分をポリゴンでクリップ/内部除去(交点tでソートし区間中点の内外で採否)
- `chainSegs`: 線分群→ポリライン連結(閉ループ検出)
- `segSegT`: 線分交差パラメータ(交差なしは null。平行は非交差扱い)
- `selfIntersects`: 閉ポリゴンの自己交差判定(非隣接辺ペアの総当たり。§3.5)。**UI層に置く**(エンジンの純関数区間には入れない)
- `stallPolyAt`: 車路の位置・向きから車室ポリゴンの4頂点を作る(§4.3の式)
- `edgeBounds` / `nearPolyEdge`: 境界距離判定の高速版(§11.2)。`nearPolyEdge(p,poly,eb,thr)` は
  `distToPolyEdge(p,poly) < thr` と同値だが、辺の外接矩形で遠い辺を除外し、距離の二乗で比較し、
  条件を満たす辺が1本見つかった時点で確定する。`thr<=0` は常に false

---

## 9. 受入条件(検証チェックリスト)

再構築後、以下をすべて満たすこと:

1. HTMLファイル単体をダブルクリックで開き、全機能が動作する(ネットワーク遮断状態で確認)
2. 起動直後は空。ツールバーは「敷地」が選択済み
3. ホイールズーム(カーソル中心)・中ボタンパンが全ツールで動作
4. 作図中、各線分とラバーバンドの中央に寸法(m)が表示される
5. 敷地・障害物・手動車路の確定でビューが移動・拡縮しない
6. 敷地/障害物/手動車路/下図を個別にクリアでき、全消去で4種すべて消える
7. 50m×30m程度の矩形敷地・初期値で生成し、駐車台数が表示され、**面積内訳の合計が敷地面積と±1%以内で一致**する
8. 障害物を置いた場所は車室も自動車路帯も避ける
9. 外周車室モード(初期値): 境界沿いに外向き車室が並び、**すべての車室の前面に車路幅分の空きがある**(出せない車室ゼロ)。凸角では隅後退により車室同士が干渉しない
10. 外周車路モード: 自動車路帯の端が外周車路まで延び(緑地で分断されない)、交角120°以下の突き当たりに**底辺2.000mの隅切り**が生成される(障害物が塞ぐ場合は延長しない)
11. 車室の前面(車路側)の辺が琥珀色で強調され、車の向きが判別できる
12. 不整形敷地(斜め辺を含む六角形など)でも、緑地は不透明の同一色で連続して表示される
13. DXF出力をCADで開き、(a)緑地外形線が存在する、(b)車路の線が外周車路・手動車路・障害物を突き抜けていない、(c)画面と同じ構成(SITE/GREEN/OBSTACLE/STALL/AISLE_MANUAL/AISLE/ANNOTATION層)である
14. JSON保存→読込で同じレイアウトが再現される
15. 方向の自動最適化OFF時、スライダー操作に図と台数がリアルタイム追従する
16. **右パネルの全項目に `NaN` `Infinity` `undefined` が出ない**。自己交差敷地(8の字順 `(0,0)→(40,30)→(40,0)→(0,30)`)・直線状の退化敷地(`(0,0)→(20,0)→(40,0)`)で生成しても、緑地率・原単位が `–` になること
17. **DXF読込の読み飛ばしメッセージにノイズが出ない**。CADが書き出した実データ(`DICTIONARY` `SCALE` `VISUALSTYLE` 等をOBJECTS/TABLESセクションに含むファイル)で、読み飛ばし表示が空、または実図形(`TEXT` 等)のみになること。同一ファイルで読み込める要素数(`ents`)が減らないこと
18. **PNG出力が図形全体を含む**。敷地の一部だけが見えるまでズームインした状態で出力しても図形全体が欠けず、出力後に画面の表示倍率・スクロール位置が変化しないこと。レイアウト未生成でも例外が出ないこと
19. **自己交差の警告**: 8の字順で敷地を作図すると閉合されず警告が出る。通常の凸多角形・凹多角形(L字・コの字)は従来どおり閉合できる(誤検出なし)。手動車路は自己交差する形状でも確定できる
20. **Undo/Redo**: 「全消去」→ Undo で敷地・障害物・車路が戻り、Redo で再び消える。頂点をドラッグで移動 → Undo で**ドラッグ開始前**の座標に戻る(中間位置には戻らない)。Undo 後にその頂点を再度動かしてもスタック内のスナップショットが変化しない(ディープコピー)。51回操作したあと最初の操作までは戻れない(上限50)。`Ctrl+Z`/`Ctrl+Y`/`Ctrl+Shift+Z` が効き、数値入力欄にフォーカスがあるときは横取りしない
21. **自動保存**: 敷地を作図して2秒待つと `localStorage` にJSONが入る。リロードすると復元の案内が出て、ボタンで敷地・障害物・車路・パラメータが復元される。何も作図していない状態では案内が出ない
22. **離脱警告**: 作図後にリロードを試みると離脱確認が出る。「保存」ボタン直後は出ない。図形が空のときは出ない
23. **行き止まり警告**: 車路帯の端が障害物等で外周車路に届かない形状で「⚠ 行き止まりの車路が○箇所あります」が表示される。
    単純な矩形敷地(40×30m・外周車室/外周車路)では**表示されない**(誤検出なし)。障害物干渉の警告と**同時に2行表示**される
24. **入力検証**: 車室幅を空欄・0・99・文字列にしても**ブラウザが固まらず**生成が完走し、値が是正されて通知が出る。
    入力途中(`input`)では書き換わらない。正常値(2.7 等)は書き換わらない。
    不正なJSON(座標が文字列・点数不足・パラメータ範囲外)を読み込んでも例外を出さず、除外・修正件数が通知される

## 10. 実装上の注意

- 面積・帯判定は「回転フレーム(敷地重心周りに−θ回転)」で行い、描画・出力時に+θで戻す。回転パラメータ(重心c, θ)は結果オブジェクトに保持する
- 方向×オフセットの全探索は数百ケースになる。1案の評価はO(セル数×ポリゴン辺数)に収め、生成全体を数百ms以内に(重い後処理[帯延長・隅切り・面積集計]は**表示する案にのみ**適用する)
- **高速化は「探索を間引く」のではなく「1回の判定を速くする」方向で行う**(§11.1で前者は台数が減ることを実証済み)。
  有効だったのは ①外接矩形による早期棄却 ②距離の二乗比較(`Math.hypot` 排除) ③早期確定 ④一時オブジェクト生成の排除
  ⑤内外判定のラスタライズ化。いずれも結果は不変で、合計3〜5倍(面積集計は最大42倍)速くなる(§11.2)
- 最適化の際は**旧実装を同一ページに読み込み、全出力(座標まで)を比較して同一性を確認すること**。台数だけの比較では不十分

### 10.1 実装しないもの(過去に存在した死んだコード)

再構築の際に**作らないこと**。いずれも実装済みだが一度も使われなかったもので、削除済み。

| 対象 | 内容 | 備考 |
|---|---|---|
| モードB(`generateB`) | 手動車路の両側に車室を自動充填する別アルゴリズム(約44行) | 呼び出し箇所ゼロ。UIに切替手段もない |
| `updateCounts()` | 空関数。8箇所から呼ばれていた | 左パネルの入力状況表示(§2.1の2)を作らない方針のため不要 |
| `underlay.snapPts` | DXF全エンティティの全点をコピーした配列 | スナップは `underlay.ents` を直接走査するため未参照。大きなDXFで無駄なメモリを確保していた |

- 配置モードは自動配置のみだが、`state.mode`(常に `'A'`)と `buildExportModel(…, mode)` の
  `mode` 引数・`isA` 判定は**残す**(出力モデル構築を純関数として汎用に保つため)
- 数値許容差: 幾何判定 1e-6〜1e-9、線分連結 1e-4、グリッド集計 0.5m
- UI文言は日本語。免責注記(「計画初期の概算検討用。法規・条例・車両軌跡の検証は対象外」)を左パネル末尾に記載する。
  対象外の範囲は §12 と整合させること(車いす使用者用区画を含む)
- **退化入力への耐性**: 面積0の敷地(自己交差・直線状)でも例外を投げず、表示が壊れないこと(§2.3のガード、§3.5の警告)

---

## 11. 性能の基準値(回帰チェック用)

初期パラメータ(車室2.5×5.0m・90°・車路6.0m・**外周車室**・緑化1.0m・自動最適化ON)での実測値。配置アルゴリズムに手を入れる改修では、**台数がこの値を下回ってはならない**。

| ケース | 敷地 | 台数 | tried | generateA | computeAreas |
|---|---|---|---|---|---|
| 40×30m | 1,200㎡ | 42 | 384 | 6 ms | 2 ms |
| 150×100m | 15,000㎡ | 680 | 384 | 149 ms | 9 ms |
| complex12(12頂点) | 9,358㎡ | 370 | 672 | 270 ms | 13 ms |
| 40×30m(**外周車路**) | 1,200㎡ | 24 | 384 | 10 ms | 1 ms |

配置方向スライダー(自動最適化OFF)の1コマ: complex12 で **27ms(約38fps)**。

> 計測は `generateA` / `computeAreas` を直接呼んで行う(いずれもグローバル関数)。
> 台数・tried は探索の仕様で決まる固定値。所要時間は機械により変動するため、
> **改修の前後は必ず同一環境で比較すること**(同一ページ内に旧実装を読み込んで交互計測するのが確実)。
> 上表は §11.2 の高速化を適用した後の値。適用前は順に 13 / 378 / 939 ms(generateA)、5 / 391 / 132 ms(computeAreas)、スライダー158ms。

### 11.1 検証済み: オフセット探索の2段階化は不可(台数が減る)

高速化のため「粗探索(刻み `P/8`)で最良オフセットを求め、その ±`P/8` を 0.5m 刻みで細探索する」2段階方式を実装し、
**6形状 × 11パラメータ = 66通り**で全探索と台数を比較した結果、**採用不可**と判断した。

| 戦略 | 台数が減った件数 | 合計損失 | 最悪 | 評価回数の削減率 |
|---|---|---|---|---|
| 粗 `P/8` → 最良1点を細探索(当初案) | **5 / 66** | 11台 | **−4台** | 2.14倍 |
| 粗 `P/8` → 上位2点を細探索 | 1 / 66 | 4台 | −4台 | 1.49倍 |
| 粗 `P/8` → 上位3点を細探索 | 0 / 66 | 0台 | 0 | **1.16倍**(効果なし) |
| 粗 `P/6` → 上位2点 | 2 / 66 | 5台 | −4台 | 1.20倍 |
| 粗 `P/8` → 上位2点 → 中間1m → 0.5m | 3 / 66 | 7台 | −4台 | 1.95倍 |

最悪ケースは**単純な40×30m矩形・外周車路モードで 24台 → 20台(−17%)**。
台数を保てるのは上位3点まで細探索する場合のみで、そのときの高速化は1.16倍にとどまり実質的な効果がない。

> **結論**: 探索の2段階化は行わない。台数の維持を優先し、0.5m刻みの全探索を維持する。
> なお §11 の3ケース(いずれも外周車室モード)だけでは台数の減少が現れないため、
> **探索を変更する改修を検証する際は、外周部の扱い・駐車角度・敷地形状を組み合わせた多数ケースで比較すること。**

### 11.2 高速化(実施済み) — 探索範囲・判別ルールを変えない最適化

§11.1 のとおり探索を間引く方法は使えないため、**結果を1ビットも変えない**実装最適化のみで高速化する。
以下は実測でホットスポットを特定して適用したもの。

**(a) 境界距離判定(`makeUsableTest` 内) — `generateA` の最大のボトルネック**

改修前は車室候補1つにつきサンプル点9個 × 全辺で `distPtSeg`(`Math.hypot`)を呼び、
complex12 の1回の生成で **`distPtSeg` が3,566万回**発生していた。次の3点で置き換える(判定内容は同値):

1. **辺の外接矩形で早期除外** — 各辺の `[xmin,xmax,ymin,ymax]` を前計算し、点が `thr` 以上離れた辺は距離計算せず飛ばす
2. **距離の二乗で比較** — `Math.hypot` を排除(`d < thr` ⟺ `d² < thr²`)
3. **早期確定** — `min` を全辺で求めず、`thr` 未満の辺が1本見つかった時点で確定する

**(b) サンプル点の配列生成をやめる** — `samplePts()` は候補ごとに9点の配列とオブジェクトを新規生成しており、
complex12 の1回の生成で94万回(約840万オブジェクト)。使い回しの1オブジェクトで走査し、GC負荷を除去する。
**判定は論理積のため、点を見る順序を変えても結果は同一。**

**(c) 敷地・障害物・手動車路の外接矩形による早期棄却** — 矩形外の点はポリゴン内部になり得ないため `ptInPoly` を省略できる

**(d) `computeAreas` のラスタライズ化 — スライダー応答性の最大要因**

改修前は「全セル × 全車室」で `ptInPoly` を回しており、complex12 で **992万回**(370車室 × 3.7万セル)。
これを**ポリゴンごとに外接矩形内のセルだけを走査してフラグ配列へ書き込む**方式(ラスタライズ)に変更し、
セル走査ではフラグを参照するだけにする。**外接矩形の外は内部になり得ないため分類結果は完全に同一。**

- 障害物・手動車路・車室・隅切りをそれぞれ `Uint8Array(nx*ny)` のフラグへ展開
- **分類の優先順位(§6の1〜10)は一切変更しない**
- 帯判定の回転はセルごとに `Math.sin`/`Math.cos` を呼ばず、`sin`/`cos` を1回だけ求めて使い回す
- `perimeterMode` / `perimeterDepth` はループ外へ持ち上げる
- `ptInPoly` 992万回 → **6.7万回**

**効果(同一環境・交互計測の中央値)**

| ケース | generateA | computeAreas | 合計 |
|---|---|---|---|
| 40×30 | 13→6 ms (2.15倍) | 5→2 ms (2.8倍) | 18→8 ms (2.30倍) |
| 150×100 | 378→149 ms (2.54倍) | 391→9 ms (**41.6倍**) | 769→158 ms (4.86倍) |
| complex12 | 939→270 ms (3.48倍) | 132→13 ms (**10.1倍**) | 1071→283 ms (3.79倍) |

配置方向スライダーの1コマ: **158ms(約6fps) → 27ms(約38fps)**。

**結果同一性の検証(必須)**

最適化は「結果を変えない」ことが前提のため、旧実装を同一ページに読み込んで全出力を比較する。

- `generateA`: **99通り**(7形状 × 11パラメータ + 障害物・手動車路あり3構成)で
  台数・`tried`・採用方向・オフセット・**全車室ポリゴンの座標(小数9桁)**・全車路帯の座標が一致
- `computeAreas`: **15通り**で 緑地・車路・車室・障害物・島面積・`step`・**緑地セルの全座標**が一致
  (最大10,872セルのケースを含む)
- 差異は**0件**

### 11.3 残る高速化の選択肢(未実施)

- **Web Worker 化** — 総時間は変わらないがUIのフリーズを完全に解消できる。
  `generateA` が数百ms残るため、大規模敷地では依然として体感の引っかかりがある
- **`computeAreas` の境界距離判定** — 残るコストは `distToPolyEdge`(セルごと・全辺)。
  §11.2(a)と同じ手法で高速化できるが、`Math.hypot` と二乗比較では境界上のセルで
  丸め結果が変わり得る(面積が0.25㎡単位で動く可能性)ため、適用するなら再度全出力の一致検証を行うこと

---

## 12. 対象外の機能(保留中・未実装)

以下は**実務上の要望として認識しているが、現時点では意図的に実装していない**。
いずれも規模が大きく仕様の決定に判断を要するため、**着手前に方針を確認し承認を得ること**。
現行版は「これらを含まない計画初期の概算検討用」であり、左パネル末尾の免責注記もその前提で書かれている。

> **状態: 2026-07-27 時点で保留。** 依頼者の判断により着手を見送っている。

### 12.1 車いす使用者用駐車区画への対応

- 幅 **3.5m**(通常2.5m)の区画。**附置義務条例でほぼ必ず要求される**が、現在は完全に対象外
- 検討を要する事項:
  - 必要台数の指定方法(台数を直接指定するか、総台数に対する比率か)
  - 配置位置の優先順位(建物出入口に近い外周部を優先するか)
  - 通常区画との干渉調整(外周車室の中央寄せ配置との整合)
  - 台数の内訳表示(通常○台＋車いす用○台)
  - DXF出力での別レイヤ化

### 12.2 DXF の INSERT(ブロック参照)対応

- **実務のDXFは敷地線がブロック内にあることが多く、その場合ジオメトリが丸ごと失われる**
  (現行は `INSERT` を読み飛ばし件数として報告するのみ。§7.1)
- 検討を要する事項:
  - `BLOCKS` セクションの解析、`INSERT` の挿入点・尺度・回転の適用
  - ネストしたブロックの扱い
  - **同梱サンプル(`SiteLine01.dxf` / `ParkingLayout01.dxf`)には INSERT が含まれないため、別途テストデータが必要**
- 併せて `SPLINE` / `ELLIPSE` の折れ線近似も検討対象(現在は読み飛ばし)

### 12.3 タッチ／ペン入力対応

- 現在は**マウスイベントのみ**のため、タブレットでの現地検討に使えない
- 検討を要する事項:
  - `pointerdown`/`pointermove`/`pointerup` への移行
  - 2本指のピンチズーム・パン
  - タップとドラッグの判別しきい値
  - スナップ許容半径のタッチ向け拡大(現在は9px相当)

---

## 13. 改版履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 初版 | — | 再構築用の要件・仕様を記述 |
| 第2版 | 2026-07-27 | 不具合修正を反映: 緑地率等の0除算ガード(§2.3)、自己交差の検出・警告(§3.2/§3.3/§3.5/§8)、DXF読込のセクション追跡と読み飛ばし報告の限定(§7.1)、PNG出力を図形全体・2倍解像度に変更(§7.3)。受入条件16〜19、性能基準値(§11)を追加 |
| 第3版 | 2026-07-27 | 作業内容の保護を反映: Undo/Redo(§2.2/§3.6)、自動保存・復元・離脱警告(§7.4)、保存JSONの構築/復元を関数化(§7.3)。受入条件20〜22を追加 |
| 第4版 | 2026-07-27 | オフセット探索の2段階化を検証し**不採用**と決定(§4.2の注記、§11.1に66通りの実測データ)。高速化の残課題を整理。コードは変更なし(全探索を維持) |
| 第5版 | 2026-07-27 | 結果を変えない実装最適化を実施(§11.2): 境界距離判定の外接矩形除外・二乗比較・早期確定、サンプル点の配列生成排除、面積集計のラスタライズ化。generateA 3.5倍・computeAreas 10〜42倍・スライダー6fps→38fps。§6/§8/§10/§11の実測値と方針を更新。**出力は99通り+15通りの比較で完全一致(差異0件)** |
| 第6版 | 2026-07-27 | 計画品質チェックと入力検証: 行き止まり車路の検出(§4.5)と警告表示の複数行化(§5.1)、数値入力・JSON読込の検証(§2.4)。受入条件23〜24を追加。配置結果は36通りの比較で完全一致 |
| 第7版 | 2026-07-27 | 死んだコードを削除(§10.1): モードB(`generateB`)・空関数 `updateCounts()`・未参照の `underlay.snapPts`。配置結果・面積集計は36通りの比較で完全一致。**対象外の機能(車いす用区画・DXF INSERT・タッチ入力)を§12として明文化し、保留中であることを記載** |
